export function debugf(tag, message) {
  console.log(`${tag}: ${message}`)
}

export function infof(tag, message) {
  console.log(`${tag}: ${message}`)
}

export function warnf(tag, message) {
  console.log(`${tag}: ${message}`)
}
